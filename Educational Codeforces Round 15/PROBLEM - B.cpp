#include <iostream>
#include <math.h>
using namespace std;

int main() {
	int size;
	cin >> size;
	int array [size];
	for (int i = 0; i < size; i++){
        cin >> array [i];
    }
	int a, b, answer;
	int min;
	min = 0;
	for (int i = 0; min < size; i++){
        array [min] = a;
        int power;
        power = a + array [i]/ 2);
        if power % 2 == 0}
           answer += 1;
        }
        if (i < size){
              min += 1;
        }                             
	cout << answer;
	return 0;
}
